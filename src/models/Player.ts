export class Player {
  key: string;
  title: string;
  email: string;

  constructor(key: string, title: string, email: string) {
    this.key = key;
    this.title = title;
    this.email = email;
  }

}
